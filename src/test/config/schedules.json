{
  "schedulerInfoSet": [
    {
      "enable": true,
      "job": {
        "group": "test job group 1",
        "jobClazz": "com.zt.serviceListener.schedule.TestJob_1",
        "name": "test job 1"
      },
      "triggers": [
        {
          "cron": "0/2 * * * * ?",
          "group": "test trigger group",
          "name": "test at every 2 second"
        },
        {
          "cron": "0/5 * * * * ?",
          "group": "test trigger group",
          "name": "test at every 5 second"
        }
      ]
    },
    {
      "enable": false,
      "job": {
        "group": "test job group 2",
        "jobClazz": "com.zt.serviceListener.schedule.TestJob_2",
        "name": "test job 2"
      },
      "triggers": [
        {
          "cron": "0/3 * * * * ?",
          "group": "test trigger group",
          "name": "test at every 3 second"
        }
      ]
    }
  ]
}